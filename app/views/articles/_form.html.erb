<%= form_for @article do |f| %>
 
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@article.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @article.errors.full_messages.each do |msg| %>
          <div class="alert alert-danger" role="alert">
          <li><%= msg %></li>
          </div>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
      <%= f.text_field :title,  class: 'form-control', autofocus: true, placeholder: 'Title' %>
  </p>
 
  <p>
  <div class="form-group">
      <%= f.text_area :text, class: 'form-control', autofocus: true, placeholder: 'Text'  %>
  </p>
  </div>
 
  <p>
    <%= f.submit "Create", class: "btn btn-s btn-success" %>
    <%= link_to 'Back', articles_path, class: 'btn btn-info' %>
  </p>
 
<% end %>